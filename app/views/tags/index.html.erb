<h1>Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag.id) %>
    	<% if logged_in? %>
    		<%= link_to "delete", tag_path(tag.id), method: :delete, data: {confirm: "Really delete this tag?"} %>
		<% end %>    	
    </li>
  <% end %>
</ul>